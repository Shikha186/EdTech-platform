{
  "name": "edtech",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    
    "frontend": "npm run dev --prefix StudySphere",
    "backend": "npm run dev --prefix backend",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },

  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "concurrently": "^10.0.3"
  }
}
